# TODO

## CI/CD follow-ups (added with the new `.github/workflows/*.yml` set)

- [ ] **Zero test coverage** — `src/` has no `*_test.go` files (`tests/` only has `.gitkeep`). The new `ci.yml` `test` job enforces a 60% coverage threshold per `cicd_conventions.md`; it will fail red until real tests are added. Do not lower the threshold or disable the gate to force green — write actual tests.
- [ ] **`docker/Dockerfile.dev` missing** — AI.md's CI/CD Rules require a `build-devel` job in `docker.yml` built from `docker/Dockerfile.dev`, matching sibling repos (`api`, `pastebin`, `ipgaze`). This repo has no `Dockerfile.dev`, so `docker.yml` currently ships `build-standard` only. Create `docker/Dockerfile.dev` (MODE=devel/DEBUG=true, same builder pattern as the standard Dockerfile) and add the `build-devel` job, or update AI.md if a devel image is intentionally not wanted for this project.
- [ ] **LDFLAGS var names mismatch AI.md** — AI.md specifies `-X main.Version=... -X main.CommitID=... -X main.BuildEpoch=... -X main.OfficialSite=...`, but `src/main.go` only declares `Version`, `Commit`, `BuildDate` (no `CommitID`/`BuildEpoch`/`OfficialSite`). The existing `docker/Dockerfile` already has a latent no-op (`-X main.CommitID=...` targets a var that doesn't exist). The new workflows (`ci.yml`, `docker.yml`, `daily.yml`, `release.yml`, `beta.yml`) were built to match the real code (`main.Version`/`main.Commit`/`main.BuildDate`), not the stale AI.md convention. Reconcile: either update AI.md's documented LDFLAGS convention, or add the missing vars to `main.go` and fix `docker/Dockerfile`'s build-arg wiring to match.
- [ ] **Dockerfile builder image mismatch** — AI.md's Docker Rules mandate the builder stage use `casjaysdev/go:latest`; the actual `docker/Dockerfile` uses `FROM golang:alpine AS builder`. Reconcile one way or the other.
